import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const DetailsScreen = (props) => {
  return (
    <View>
      <Text> details here bro </Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default DetailsScreen;
